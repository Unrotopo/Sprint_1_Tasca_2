import objects.Cinema;

public class Main {
    public static void main(String[] args) {
        Cinema cinema = new Cinema();
        cinema.start();
    } //Cambiar objects por entities (en todos)
    // CASO 4 (elimina la que le da la gana, el primero de la lista)
    // CASO 5 (elimina el primero de la lista) NO FUNCIONA!!
    // Eliminar funciones que no se usen (getters y setters incluidos?)
    // Pasar todo lo posible a public (no static)
}
